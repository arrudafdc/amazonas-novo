import styles from "./Slogan.module.css";

export function Slogan() {
  return (
    <main id="produtos" className={`${styles.slogan} container`}>
      <h1>Produtos para tudo e todos.</h1>
    </main>
  );
}
